{
  "name": "bickford-live-filing",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\" \"npm run dev:worker\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:worker": "npm run dev --workspace=apps/worker",
    "db": "docker compose up -d",
    "db:init": "docker compose exec -T postgres psql -U bickford -d bickford < init.sql",
    "db:down": "docker compose down",
    "build": "npm run build --workspace=apps/web",
    "build:all": "npm run build --workspace=apps/web && npm run build --workspace=apps/api && npm run build --workspace=apps/worker"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
