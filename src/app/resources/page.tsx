import { StaticPage } from "@/components/pages/StaticPage";

export default function ResourcesPage() {
  return (
    <StaticPage
      title="Resources"
      subtitle="Playbooks, docs, and operator checklists"
      paragraphs={[
        "Access operating checklists, packet documentation, and safety runbooks in one place. This page is a placeholder to prevent 404s while we centralize the resource library.",
        "For immediate assistance, contact support@bickfordtechnologies.com.",
      ]}
    />
  );
}
